name: NodeJS with Webpack

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x]
        include:
          - page-path: '9-JavaScript-browser/2-DOM/hit-game/'
            page-url: 'hit-game'
            

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}

    - name: Make folder
      run: mkdir -p /home/runner/work/_temp/dist/${{ matrix.page-url }}

    - name: Build
      run: |
        echo ${{ matrix.page-path }}dist/
        echo ${{ steps.deployment.outputs.page_url }}${{ matrix.page-url }}
        cd ${{ matrix.page-path }}        
        npm install webpack webpack-cli html-webpack-plugin
        npx webpack
        mkdir -p /home/runner/work/_temp/dist/${{ matrix.page-url }}
        cp -r dist/* /home/runner/work/_temp/dist/${{ matrix.page-url }}
        
    - name: Upload generated files as artifact
      uses: actions/upload-pages-artifact@v3
      id: deployment
      with:
        path: ${{ github.path }}dist
          
  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    steps:
    - name: Deploy to GitHub Pages
      uses: actions/deploy-pages@v4
      id: deployment
